# 渲染距离（以区块为单位）
RENDER_DISTANCE = 1.5

# 视野
FOV = 90

# --------------------------------- 性能 --------------------------------- 

# 每帧的每个区块中最大子区块更新数
CHUNK_UPDATES = 4

# --------------------------------- 质量 --------------------------------- 

# 环境光遮蔽和平滑光照
SMOOTH_LIGHTING = True
# 平滑光照通过对每个顶点的光线进行线性插值来实现，从而在每个片段上实现光的平滑效果
# 它还添加环境光遮蔽，以模拟被不透明对象阻挡的光线
# 此功能会严重影响区块更新/构建速度
