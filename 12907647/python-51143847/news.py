ij最新版

'''
*dev:
    1.新结构解释器
    2.re扫描
    3.四则运算
    4.变量
*0.0.0 -clown:
    1.基础运算
    2.if-else
    3.while:break/continue-else
0.0.1 -turing (test):
    1.研发public_block，区分条件、循环的作用域
    2.函数(含call)
0.0.2 -turing (test):
    1.函数返回值
    2.测试支持递归
    3.测试支持函数套函数
    4.支持字符串及转义
*0.1.0 -turing:
    1.完成图灵测试
    2.支持字符串、列表
0.1.1 -turing (repack1):
    支持元组（后面几版为了氵版本而氵制作（
0.1.2 -turing (repack2):
    支持索引
0.1.3 -turing (repack3):
    支持修改索引
0.1.4 -turing (repack4):
    1.对parser的改进
    2.完善更新日志news.py的编写
*0.1.5 -not_lisp (dev):
    1.修改赋值逻辑
    2.解决不应该转义的字符被错误转义的问题
    3.parse对“class”的识别
0.1.6 -not_lisp (dev):
    parse对“for”的识别
0.1.7 -not_lisp (dev):
    将“for”改为“Cycle”，并添加interpreter对其的识别
0.1.8 -not_lisp (test-α):
    1.添加scanner对分号的识别
    2.修改cycle语法，令格式接近c++的for循环
    3.parse对import/export/match的识别
    4.添加字典类型，及interpreter的识别
    5.使用类框架，尝试完善对class和object的基础支持，但失败
0.1.9 -not_lisp (test-α):
    重构关于class的代码，object用dict代替，但仍然存在问题
0.1.10 -not_lisp (test-β):
    添加interpreter中字典的返回
0.1.11 -not_lisp (test-γ):
    重构关于class的代码，重新使用call __init__代替原情况
    大更新，实现了类和对象！
    悲惨事件: 0.1.8版本的更新没保存，所以丢失了，下次重写
    (感谢 @吴宇航 的示例教程)
0.1.12 -not_lisp (test-δ):
    未更新
0.1.13 -not_lisp (test-ε):
    未更新
'''