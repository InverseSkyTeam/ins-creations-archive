#include <iostream>

/*
import std;

auto print() -> void
{
    for (std::size_t i = 1; i <= 9; i++)
    {
        for (std::size_t j = 1; j <= i; j++)
            std::print("{}*{}={}\t", i, j, i * j);
        std::println("");
    }
}

*/
auto main() -> int
{
    // print();
    std::cout << "来自C++23的乘法表!用Visual Studio 2022 Preview能过编译！" << std::endl;
    return 0;
}
